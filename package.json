{
  "name": "uacp",
  "version": "0.6.0",
  "description": "Unified AI Context Protocol — schema validator and test harness",
  "type": "commonjs",
  "scripts": {
    "validate": "node validate.js",
    "test": "node test/version-consistency.js && node --test test/resource-limits.test.js && node --test tools/uacp-validate/__tests__/*.test.js && node validate.js && node conformance/harness/run.js",
    "test:context-sharing": "node conformance/harness/run.js context-sharing --vectors conformance/vectors/context-sharing/event-chains/"
  },
  "devDependencies": {
    "@noble/ed25519": "^3.1.0",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "canonicalize": "^3.0.0",
    "js-yaml": "^4.1.0"
  }
}
