{
  "name": "uacp",
  "version": "0.5.0",
  "description": "Unified AI Context Protocol — schema validator and test harness",
  "type": "commonjs",
  "scripts": {
    "validate": "node validate.js test-vectors/*.json",
    "test": "node test/version-consistency.js && node validate.js && node conformance/harness/run.js"
  },
  "devDependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1"
  }
}
